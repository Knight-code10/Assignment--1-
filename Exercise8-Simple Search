names=["Jake","Zac","Lan","Ron","Sam","Dave"] #type the names

Search_name = input("Enter the name you want to search:") #Write the input you want to add

if Search_name in names:
    print(f"{Search_name} is found in this list!") #If the name is found the output is found
else:
    print(f"{Search_name} is not Avaiable") #Otherwise Not found
